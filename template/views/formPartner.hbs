{{>header}}
<div class="container">
  <!--Error message -->
  {{#if errorMsg}}
    {{>messageError}}
  {{/if}}
  <!--Success message -->
  {{#if successMsg}}
    {{>messageSuccess}}
  {{/if}}


      {{#if contact}}
      <div class="card">
        <div class="card-header bg-secondary h5">
          <h5 class="card-title">{{#if partner}} Socio {{else}} Aún no es socio {{/if}}</h5>
        </div>
      </div>

      <div class="card-body bg-dark">
        <!--ROW 1-->
        <div class="form-row">
          <div class="col">
            <label for="labelLastName" class="text-light">
              {{contact.fullName}} -
              <a class="btn btn-link"
                href="/contact?cellPhoneToSearch={{contact.cellPhoneNumber}}"
                role="button">{{contact.cellPhoneNumber}}
              </a>
            </label>
          </div>
        </div>
      </div>
      </br>
      {{>screenServicePlace contact=contact servicePlace=servicePlace}}
      {{>screenItem}}
      {{/if}} <!--end if contact-->
</div><!--end container-->
{{>footer}}
